// ===== Course Enrollment System JS =====

// Wait until page loads
document.addEventListener("DOMContentLoaded", initApp);

function initApp() {
    console.log("Application Initialized");
   
}

// ===== Utility Function  =====
function showMessage(message) {
    alert(message);
}
